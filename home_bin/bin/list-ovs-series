#!/bin/sh
# TODO: this prints errors like
# You should provide the server version in the URL configured via git-config or --server
git pw --project openvswitch series list --submitter=ihrachys@redhat.com
